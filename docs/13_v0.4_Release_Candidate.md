# v0.4 Release Candidate — Longbridge A-share live data

> Status: **Tencent in-session live gate PASSED (2026-08-28 11:17–11:23 CST).** Sina one-shot cross-check PASSED. Longbridge optional (skipped without credentials). Tushare excluded.
>
> Package versions remain **0.3.0**. Protocol remains **1**. Do **not** tag `v0.4.0` until Core wiring/release is explicitly completed.
>
> Token target: **0**. No LLM.

This file is the M7 stop report.

---

## 1. Commits / git

Branch: `feat/v0.4-live-market-data` (not merged, not pushed).

| SHA | Message |
|---|---|
| `52f45d0` | `feat: add longbridge quote provider` |
| `528720c` | `feat: add longbridge cursor configuration` |
| (this file) | `docs: finalize v0.4 release candidate` |

Tag `v0.3.0` was not modified.

## 2. Git status

Working tree should be clean after the RC commits. `.tmp/` (including any local Longbridge wheel) is gitignored. `*.vsix` is gitignored.

## 3–5. Versions

| Item | Value |
|---|---|
| Python package | `0.3.0` |
| Cursor extension | `0.3.0` |
| Protocol | **1** (unchanged) |

## 6. Longbridge SDK

| Item | Result |
|---|---|
| Package | `longbridge==4.5.0` (optional extra `live`) |
| Windows / CPython 3.12 | **works** (`AsyncQuoteContext` imports) |
| `uv sync --extra live` | **succeeded** after the official win_amd64 wheel was available locally/cache |
| Default `uv sync` | does **not** install the SDK |
| This session live `quote()` | **not run** (credentials unset) |

## 7. Production Provider architecture

```text
CLI/Host --provider longbridge
  → LongbridgeQuoteProvider
       lazy process-lifetime AsyncQuoteContext.create
       asyncio.Lock + wait_for (native await, no to_thread)
       vendor datetime → Unix; SH/SZ filter; turnover always None
  → normalize_snapshot(clock)   # received_timestamp from Clock
  → MarketEngine ingest
       older market_ts: out_of_order + health.observe(error=...)  # does NOT refresh success
       duplicate ts: RingBuffer replace; cooldown/dedupe intact
  → Feature / Event / Signal (unchanged math)
  → Protocol v1 state/alert
```

Tencent is **not** wired. No multi-provider failover. MarketEngine does not import the SDK.

## 8–11. Semantics

| Gate | Decision | Confidence |
|---|---|---|
| Volume | pass through as session-cumulative series | **LIKELY** (official field + same-session unit consistency). Live in-session increase **PENDING** |
| Volume unit | UNKNOWN (股 vs 手) | not confirmed; delta/ratio stay same-unit |
| Turnover | **`None`** | UNIT_MISMATCH not proven on Longbridge live; Tencent bake-off was mismatch; no silent ×100 |
| Timestamp | vendor `timestamp` | DOCUMENTED; live advance **PENDING** |
| received vs market | Clock vs vendor | CONFIRMED in unit tests |

## 12. Duplicate / out-of-order

- Older `market_timestamp`: **suppress** (no buffer append, no Feature/Event). Increment `out_of_order_count`. Treat as a **failed observation** (`health.observe(symbol, error=...)`) so `last_success_mono` is not refreshed. Repeated old quotes can reach STALE / DISCONNECTED under existing `HealthPolicy` (`delayed_s=3`, `stale_s=30`, `disconnect_s=60`, `disconnect_failures=3`).
- Duplicate timestamp: RingBuffer **replace**; engine counts `duplicate_timestamp_count`; cooldown/dedupe still apply (no manufactured extra alert edge).

## 13. Session / lunch / reset

Existing UTC+8 **calendar-day** session id (no full exchange calendar):

- Lunch = **same session** (not a reset). `max_anchor_lag_s=30` already refuses morning anchors after lunch.
- New calendar day = new session. Cross-session `volume_delta` / `change_1m` are `None` (not a negative volume spike, not a 1m overnight gap move).
- Real Longbridge next-session reset: **live validation pending**. Provider does not require global volume monotonicity.

## 14. Timeout / failure

Provider error or timeout → **no new snapshot**, no Feature/Event on the stale buffer, health observes the error. Partial batch drops invalid symbols. Missing SH/SZ quote → missing-quote error. HK/US not fetched.

## 15. Rate limit

One batch `quote()` per due group. Official ≤10/s; watchlist ≤10 and HOT ~1s stay inside that. `301606` → `ProviderRateLimitError`, no retry storm. No extra distributed limiter. Scheduler **unchanged**.

## 16. Credentials

Env vars only. Redaction tests. Not in Protocol, settings defaults, VSIX, or argv. Cursor child inherits process env.

## 17. Observability

Provider: success/failure, last latency, timeout count, rate-limit count, last market/received ts, dropped symbols. Engine: out-of-order, duplicate, recovery, last fetch error. Logs on stderr. stdout remains Protocol JSONL.

## 18–19. CLI / Cursor

`--provider` / `marketSentinel.provider`: `fake` | `replay` | `longbridge`. `http` remains an explicit stub (exit 2). Provider change is restart-needed. Host does not parse vendor fields.

## 20. Fake / Replay

Unchanged. v0.2 Event/Signal and v0.3 Host tests still pass.

## 21–24. Live smoke (Tencent primary, 2026-08-28 11:17–11:23 CST)

| Item | Result |
|---|---|
| Tencent one-symbol session freshness | **PASS** (`600519.SH`; date `20260828`; `ts_raw` in morning session; volume non-decreasing with positive deltas) |
| Tencent 1 / 2 / 10 symbol live batches | **PASS** (requested symbols returned; today's timestamps) |
| Sina one-shot cross-check | **PASS** (HTTP 200; today's date; session times; price within 2% of Tencent; no polling) |
| Longbridge in-session smoke | **SKIP** (credentials UNSET; optional, not a blocker) |
| Tushare | **EXCLUDED** (realtime/minute APIs need separately granted permissions) |
| Feed LIVE vs DELAYED | `HealthPolicy.delayed_s` remains **3.0** (not widened) |
| Event false-positive sanity on live tape | **SKIP** (Tencent/Sina not wired into MarketEngine yet) |
| Offline timeout → no events; older quote does not keep LIVE | **PASS** (synthetic Longbridge path) |
| Tencent/Sina live tests skip/mock | **FAIL-CLOSED** (no skip; network required) |

```bash
uv run python -m tools.live_probe tencent --symbols 600519.SH --interval 2 --count 8
uv run python -m tools.live_probe sina --symbols 600519.SH,000001.SZ
uv run pytest -m live
```

**Do not tag `v0.4.0`**, merge, or start v0.5 until Core `MarketProvider` wiring (if required) and remaining release steps are explicitly completed.

### Freshness evidence (Tencent `600519.SH`)

- Quote date matched the A-share trading date `2026-08-28`.
- Quote times were inside the morning cash session (~11:17–11:18).
- 8-sample window volumes: `8228, 8228, 8230, 8231, 8231, 8241, 8241, 8241` (non-decreasing; 3 positive volume deltas; 4 timestamp advances).
- Price need not change; it did not stall the gate.

Sina volumes are ~100× Tencent (股 vs 手-style units). Cross-check uses price, not volume equality. Do not silent-scale in Core.

## 25–31. Automated checks (Tencent/Sina live-gate session)

| Check | Result |
|---|---|
| `uv run pytest` (no `-m live`) | **345 passed**, 5 live deselected |
| `uv run pytest -m live` | **3 passed** (Tencent 1-symbol, Tencent 1/2/10, Sina once), **2 skipped** (Longbridge, no creds) |
| `uv run pytest --cov=market_sentinel` | **92.02%** (fail_under 85) |
| `uv run ruff check .` | pass |
| `uv run ruff format --check .` | pass |
| `uv sync --extra live` | already present (longbridge 4.5.0; unused by Tencent gate) |
| Host TS checks | unchanged this session (docs/probe only) |

## 32. Docs

- `docs/06_Roadmap_v0.4_Live_Market_Data.md` — Tencent/Sina live-gate order
- `docs/10_v0.4_Provider_Source_Audit.md` — order note; Tushare still excluded
- `docs/11_v0.4_M1a_Live_Semantics_Bakeoff.md` — Tencent probe reused
- `docs/12_v0.4_Longbridge_Provider_Setup.md` — Tencent primary, Longbridge optional
- `docs/13_v0.4_Release_Candidate.md` — live evidence
- README / CHANGELOG Unreleased

## 33. Known limitations

- Tencent/Sina remain **probes**, not `MarketProvider` implementations (architecture not redesigned this step)
- Volume units still UNKNOWN; Sina volume is ~100× Tencent — do not silent-scale
- Longbridge optional; credentials UNSET in this environment (skipped, not a blocker)
- Turnover always None on the Longbridge production path
- No China exchange calendar (UTC+8 calendar-day session)
- CN LV1 delay vs 3s DELAYED policy: Tencent received−market latency often ~3s; `delayed_s` was **not** widened
- Package version still 0.3.0 until an explicit post-gate bump

## 34. Remaining UNKNOWN / PENDING

- Core `MarketProvider` wiring for Tencent (explicit follow-up; not this gate)
- Licensing / ToS for unofficial Tencent/Sina HTTP
- Regular-session high/low documentation
- Longbridge in-session volume/latency (optional; not required)

## 35. HARD STOP conditions

Tencent was reachable from this mainland network. No access-denied/anti-bot payload. Schema parsed as expected (~88 `~` fields, ticker match). Quote date/time were today's morning session. Live Tencent/Sina tests were **not** skipped or mocked. 1 / 2 / 10 batches passed. Working tree changes are the intended probe/docs/tests only.

Did **not**: version bump; merge; tag `v0.4.0`; push; start v0.5; add AKShare/Tushare; delete Longbridge; wire Tencent/Sina into MarketEngine; change `HealthPolicy.delayed_s`.

### Self-review (A–I)

| | Answer |
|---|---|
| A Core semantics frozen? | Yes. Volume still session-cumulative; Feature/Event/Scheduler untouched |
| B Protocol v1? | Unchanged |
| C Network in default tests? | No (`-m not live`) |
| D Secrets leaked? | No |
| E Stale snapshot replay? | Timeout/missing skip Feature/Event (Longbridge path unchanged) |
| F Host understands vendor fields? | No; provider name only |
| G Extra deps? | No AKShare/Tushare. Longbridge extra unused by this gate |
| H UNKNOWN treated as FACT? | No; volume units still UNKNOWN |
| I Untested failure branches? | Offline parse/gate coverage; live Tencent/Sina fail-closed |

---

**STOP.** The replacement **live gate passed**. `v0.4.0` is **not** tagged. v0.5 is **not** started. Next explicit steps (not done here): Core wiring if required, version bump, merge/tag.
