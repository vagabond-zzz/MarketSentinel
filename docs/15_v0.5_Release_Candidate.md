# v0.5 Release Candidate — Market Intelligence Router

> Status: **RC Fix Pass complete for second external review.**
>
> Branch: `feat/v0.5-market-intelligence` (not merged, not pushed).
>
> Package versions remain **0.3.0**. Protocol remains **1**. Do **not** tag `v0.5.0`.
>
> Intelligence is **off by default**. Ordinary ticks: **Token = 0**.

This file is the M7 stop report plus the RC Fix Pass. First external review accepted the architecture and **rejected merge/tag**.

---

## RC Fix Pass

| ID | Result |
|---|---|
| RC-FIX-1 stale queued / in-flight | **CLOSED.** Generation invalidation + `_store` refuses inactive ids. |
| RC-FIX-2 bounded bookkeeping | **CLOSED.** Forget maps only when retired and `pending == 0`. |
| RC-FIX-3 trading-advice parse | **CLOSED.** Deterministic EN/ZH fail-closed, not prompt-only. |
| RC-FIX-4 bootstrap fail-open | **CLOSED.** Config/auth/unknown provider → stderr WARNING, Rule-only Core. |
| RC-FIX-5 CLI lifecycle | **CLOSED.** `run` / `--once` start → ticks → idle (`--once`) → shutdown. |
| RC-FIX-6 docs vs redaction | **CLOSED.** Coordinator fallback logs call `redact_secrets`. |
| RC-FIX-7 shutdown episode leak | **CLOSED.** `shutdown()` resets queued/in-flight episode runtime state; diagnostics kept. |

Queued stale reproduction:

```text
before: 2 queued → active empty → model_calls == 2, stale_discard == 0, registry ENRICHED
after:  2 queued → active empty → model_calls == 0, stale_discard == 2, registry empty
```

In-flight HTTP that already left the process may still cost tokens. Results never reattach.

---

## 1. Branch / HEAD / git status

| Item | Value |
|---|---|
| Branch | `feat/v0.5-market-intelligence` |
| First RC HEAD | `29c9e5e` |
| RC Fix Pass | see tip after the docs commit |
| Branched from | `feat/v0.4-live-market-data` @ `6980371` (`feat: checkpoint tencent live gate with sina cross-check`) |
| Tag `v0.3.0` | not modified |
| Tag `v0.4.0` | **not created** |
| Tag `v0.5.0` | **not created** |

Working tree after this RC commit should be clean except local caches.

## 2. M0–M7

| Milestone | What landed |
|---|---|
| M0 | `IntelligenceInput` / `Result` / `Annotation` / status / fallback / episode budget. Rule `Signal` unchanged. `docs/14_v0.5_Intelligence_Contract.md` |
| M1 | Pure `need_intelligence`. INFO/NOTICE/stale/insufficient/suppressed/expired/budget skip. IMPORTANT/CRITICAL/cluster candidates. Normal replay → 0 requests |
| M2 | `IntelligenceProvider` protocol, Fake, JSON parse fail-closed, DashScope compatible-mode adapter, `IntelligenceModelSettings` |
| M3 | Async `IntelligenceCoordinator` sidecar. `MarketEngine.tick` only enqueues. Slow model does not block ticks |
| M4 | Registry statuses `not_requested`/`queued`/`running`/`enriched`/`fallback`. Diagnostics separate from feed latency |
| M5 | Protocol v1 optional `WireSignal.intelligence`. Cursor hover shows enrichment only when `enriched`. Alerts unchanged |
| M6 | Offline Fake replay eval: normal market 0 calls; significant episode 1 call; failures keep Rule Signal |
| M7 | Regression, docs, this audit. No version bump |

## 3. Commits on this branch

| SHA | Message |
|---|---|
| `497a415` | `feat: freeze v0.5 intelligence contract` |
| `ca21ecd` | `feat: add deterministic intelligence router` |
| `a58a60f` | `feat: add intelligence provider boundary` |
| `e00cec9` | `feat: run intelligence as an async sidecar` |
| `540ddd4` | `feat: keep rule signals distinct from intelligence status` |
| `49386d9` | `feat: add optional protocol v1 intelligence fields` |
| `dc4e37b` | `test: evaluate intelligence replay and failure paths` |
| `29c9e5e` | `docs: record v0.5 intelligence RC` |
| `0bff1ce` | `feat: retire stale intelligence work and fail-open the sidecar` |
| (this file) | `docs: record v0.5 RC fix pass` |

Plus v0.4 checkpoint `6980371` on the parent branch.

## 4. Main files

Added under `src/market_sentinel/intelligence/` (contract, router, coordinator, fake, DashScope adapter, bootstrap). Protocol: `ipc/dto.py`, `ipc/mapping.py`. Host: `apps/cursor-extension/src/protocol/*`, `hover/model.ts`, `hover/render.ts`. Tests: `tests/unit/intelligence/*`, `tests/integration/test_intelligence_eval.py`.

## 5. Architecture

```text
Market Provider
    ↓
Normalization
    ↓
Feature
    ↓
Event
    ↓
Signal Episode
    ↓
Deterministic Intelligence Router
    ├── NO  → original Rule Signal
    └── YES → async Intelligence sidecar
                    ↓
              optional IntelligenceAnnotation (by signal_id)
                    ↓
              Protocol v1 optional WireSignal.intelligence
                    ↓
              Cursor hover (enrichment only if enriched)
```

`MarketEngine.tick()` never `await`s the model.

## 6. Router rules

Skip (no call): expired episode; cooldown/duplicate edge (`alert_edge=False`); STALE/DISCONNECTED; missing `change_1m`+`change_5m`+`volume_ratio_5m`; INFO; ordinary NOTICE; episode budget exhausted.

Candidate: priority IMPORTANT or CRITICAL; or price+volume+breakout cluster (or ≥3 event types with price and volume).

DELAYED feed may still be a candidate. Policy is `RouterPolicy`, not the model.

## 7. Episode call budget

`EpisodeCallBudget(max_calls=1, allow_escalation_recall=False)`.

A later CRITICAL upgrade does **not** get a second call unless `allow_escalation_recall=True`.

Budget is consumed when work is **queued**, so repeats of the same `signal.id` do not enqueue again.

## 8. Async / queue / cancellation

- `asyncio.Queue(maxsize=8)`, concurrency 1
- `put_nowait`; full queue drops work (`dropped_backpressure`) and does not block tick
- `asyncio.wait_for(..., timeout_s)` around `provider.complete`
- Episode retirement bumps generation, prunes the registry, and best-effort cancels the in-flight task
- Queued stale work is discarded **before** `provider.complete` (`stale_discard`, `model_calls` unchanged)
- In-flight stale work: cancel is best-effort; if a result still arrives it is discarded and must not resurrect the registry
- `shutdown()` cancels workers and in-flight tasks, detaches the queue, and resets episode runtime maps (`_calls` / `_generation` / `_pending` / registry / `_active_ids`). Diagnostics counters are kept. Restart after shutdown is not polluted by the previous call budget.
- Already-issued HTTP may still consume tokens; we do not claim Token = 0 for that case. Completions must not reattach after shutdown.
- daemon `run()` and CLI `run` start/stop the sidecar
- Default daemon does not construct a coordinator unless `MARKET_SENTINEL_INTEL_ENABLED` is set
- Missing key / unknown provider: sidecar disabled, Core continues

## 9. Fallback matrix

| Failure | `FallbackReason` | Rule Signal |
|---|---|---|
| timeout | `timeout` | kept |
| malformed JSON / trading-advice text | `malformed` | kept |
| HTTP 429 | `rate_limited` | kept |
| transport / unavailable / auth | `unavailable` / `transport` | kept |
| not a candidate / stale / budget | skip, no call | kept |

Host does not show fallback error copy. Original title/summary remain.

## 10. Protocol v1 additive changes

`PROTOCOL_VERSION` remains **1**.

`WireSignal` may include optional `intelligence`:

```text
status: queued | running | enriched | fallback
summary?, reason?, confidence?, fallback_reason?, worth_highlight?
```

Omitted when `not_requested`. Old fixtures without the field still parse.

`WireAlertCandidate` is unchanged (no extra toast).

## 11. Cursor presentation

Rule title/summary always shown. Enrichment line only if `status === "enriched"` and summary is non-empty. Fallback: original Rule Signal only. Hover/click/toast do not call a model or re-run the router.

## 12. Privacy / secrets

Model payload allowlist: `signal_id, symbol, family, direction, priority, event_types, change_1m, change_5m, volume_ratio_5m, above_vwap, rsi14, day_range_position`.

Never sent: RingBuffer, ticks, `SignalTrace`, workspace, Cursor conversation, `DASHSCOPE_API_KEY`.

Key from env `DASHSCOPE_API_KEY` only. Coordinator fallback logs run exception text through `redact_secrets` (`[redacted]`). DashScope adapter errors stay generic (no key, no provider body). Not in Protocol, argv, or git.

Enable:

```text
MARKET_SENTINEL_INTEL_ENABLED=1
MARKET_SENTINEL_INTEL_PROVIDER=dashscope   # or fake
MARKET_SENTINEL_INTEL_MODEL=qwen3.7-max-2026-06-08   # optional override
DASHSCOPE_API_KEY=...
```

## 13–15. Replay evaluation (offline Fake, not live LLM)

| Scenario | Result |
|---|---|
| `normal_market.jsonl` | `model_calls == 0` |
| `price_volume_breakout.jsonl` | `model_calls == 1`, payload ⊆ allowlist |
| same episode follow-up ticks | no second call |
| timeout / malformed / rate limit | Rule Signal kept, `fallback_count == 1` |
| STALE after `rapid_move` | no additional calls |
| slow fake model | replay ticks finish |

Normal-market **actual intelligence call count: 0**.

## 16. Python gates (RC Fix Pass)

| Check | Result |
|---|---|
| `uv run pytest` | **425 passed**, 5 live deselected |
| `uv run pytest --cov=market_sentinel` | **90.59%** (fail_under 85) |
| `uv run ruff check .` | pass |
| `uv run ruff format --check .` | pass |

Coverage was ~**92.02%** on the v0.4 baseline and **90.76%** on the first v0.5 RC. **90.59%** after the fix pass is from additional coordinator invalidation / cancel branches that default tests only partially exercise (in-flight HTTP cancel vs completion). Gate is still 85%.

## 17. TypeScript gates

| Check | Result |
|---|---|
| `pnpm test` | **145 passed** |
| `pnpm typecheck` | pass |
| `pnpm lint` | pass |
| `pnpm build` | pass |

VSIX packaging was not re-run; Protocol remains v1 additive. Re-package before a real host release.

## 18. Known limitations / UNKNOWN / PENDING

- Default daemon/CLI does not call a model (must set `MARKET_SENTINEL_INTEL_ENABLED`)
- Intelligence config failure disables the sidecar; Core stays Rule-only (no strict-fail mode in v0.5)
- No live DashScope smoke in default CI (would need `live` marker + key + network)
- Queue-full backpressure is counted but rarely hit at watchlist size 10
- Parse latency is recorded when the DashScope adapter runs parse; Fake reports 0
- Trading-instruction parse is fail-closed for listed EN/ZH phrases; bare `long`/`short` are only matched in collocations (`go long`, `long position`) to avoid `long-term` / `short-term`
- In-flight HTTP that the transport cannot abort may still consume tokens after episode retirement
- DashScope HTTP error-path coverage is partial by design
- CLI `--once` with fake provider often has no IMPORTANT episode; idle still drains any queued work

## 19. Open v0.4 items (not v0.5 completions)

These remain from the v0.4 live-gate / RC baseline and are **not** closed by v0.5:

- Tencent / Sina are still **probes**, not `MarketProvider`
- Full live Provider → Core → Protocol → Cursor path is **not** closed
- `v0.4.0` is **not** tagged
- Longbridge is optional; credentials were unset in the live-gate environment
- Volume units Tencent vs Sina still UNKNOWN; do not silent-scale
- `HealthPolicy.delayed_s` remains 3.0

## 20. Self-review A–L

| | Answer |
|---|---|
| A Model on high-frequency market path? | No. Tick only `observe_tick` / enqueue |
| B Ordinary tape Token = 0? | Yes. Default off; `normal_market` eval `model_calls==0` |
| C Deterministic Signal fact source? | Yes. `Signal` fields unchanged; annotation by `signal_id` |
| D Model failure can drop Signal/Alert? | No. Fallback keeps Rule Signal and alert candidates |
| E Same episode repeat Token burn? | No. Default budget 1; escalation recall off |
| F Host makes market/intelligence decisions? | No. Host only displays Core fields |
| G Leak ticks / workspace / conversation / secrets? | No. Allowlist payload; key from env only |
| H Protocol v1 compatible? | Yes. Optional field; old fixtures parse |
| I Unrelated Provider/Feature/Event/Scheduler changes? | No. `delayed_s` still 3.0. Tencent not wired |
| J Default tests hit model network? | No. Fake only; live marker still deselected |
| K Unbounded queue / shutdown leak? | Bounded queue 8; retired maps forgotten after pending drains; `shutdown()` clears unconsumed episode state so restart is clean |
| L Trading advice? | Parser fail-closed on EN/ZH instruction phrases; system prompt also forbids it |

---

```text
STOP — v0.5 RC-FIX-7 complete for final external approval.

Did NOT:
- merge
- push
- tag v0.5.0
- bump version
- start v0.6
```
